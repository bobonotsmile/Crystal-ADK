# crystal-adk v0.2.0 函数职责稿

## `Session/AgentSession.cs`

```csharp
public sealed class AgentSession
{
    // 底层 provider 抽象；Session 只依赖接口，不依赖具体实现
    private readonly IChatProvider _provider;

    // 会话历史操作入口；负责消息增删改导出
    private readonly SessionMessageManager _messageManager;

    // 注入 provider 与 message manager
    public AgentSession(
        IChatProvider provider,
        SessionMessageManager messageManager)
    {
        _provider = provider;
        _messageManager = messageManager;
    }

    // 对外暴露历史操作入口
    public SessionMessageManager History => _messageManager;

    // 对外暴露当前消息历史只读视图
    public IReadOnlyList<RuntimeMessage> Messages => _messageManager.Messages;

    // 非流式入口：
    // 1. 追加 user
    // 2. 导出请求消息
    // 3. 调 provider.CompleteAsync
    // 4. 收到 assistant（正文 + thinking）
    // 5. 追加 assistant
    public async Task<RuntimeMessage> RunAsync(
        string userInput,
        CancellationToken cancellationToken = default)
    {
        // ...
    }

    // 文本流入口：
    // 1. 追加 user
    // 2. 导出请求消息
    // 3. 调 provider.StreamAsync
    // 4. 同时累积 fullText / fullThinking
    // 5. 对外 yield TextChunk（正文 + thinking）
    // 6. 流结束后追加 assistant（正文 + thinking）
    public async IAsyncEnumerable<TextChunk> StreamTextAsync(
        string userInput,
        [System.Runtime.CompilerServices.EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        // ...
    }

    // 清空历史消息
    public void Reset()
    {
        // ...
    }

    // 构建请求消息：
    // 当前实现为直接导出全部历史
    private List<RuntimeMessage> BuildRequestMessages()
    {
        // ...
    }
}
```

## `Session/SessionMessageManager.cs`

```csharp
public sealed class SessionMessageManager
{
    // 实际历史消息存储
    private readonly List<RuntimeMessage> _messages = new();

    // 空历史初始化
    public SessionMessageManager()
    {
    }

    // 带初始消息的历史初始化；对 seed 做拷贝
    public SessionMessageManager(IEnumerable<RuntimeMessage> seedMessages)
    {
        // ...
    }

    // 对外暴露消息只读视图
    public IReadOnlyList<RuntimeMessage> Messages => _messages;

    // 追加任意消息；内部会做 clone
    public void Add(RuntimeMessage message)
    {
        // ...
    }

    // 追加 system
    public void AddSystem(string content)
    {
        // ...
    }

    // 追加 user
    public void AddUser(string content)
    {
        // ...
    }

    // 追加 assistant 正文
    public void AddAssistant(string content)
    {
        // ...
    }

    // 追加 assistant 正文 + thinking
    public void AddAssistant(string content, string? thinkingContent)
    {
        // ...
    }

    // 导出消息副本，供 session/provider 构建请求使用
    public List<RuntimeMessage> Export()
    {
        // ...
    }

    // 更新首条 system 消息
    public bool UpdateFirstSystem(string newContent)
    {
        // ...
    }

    // 更新最新 user 消息
    public bool UpdateLatestUser(string newContent)
    {
        // ...
    }

    // 更新最新 assistant 消息正文
    public bool UpdateLatestAssistant(string newContent)
    {
        // ...
    }

    // 删除最新 user 消息
    public bool RemoveLatestUser()
    {
        // ...
    }

    // 删除最新 assistant 消息
    public bool RemoveLatestAssistant()
    {
        // ...
    }

    // 清空全部历史
    public void Clear()
    {
        // ...
    }

    // 角色定向更新辅助函数
    private bool UpdateLatestByRole(string role, string newContent)
    {
        // ...
    }

    // 角色定向删除辅助函数
    private bool RemoveLatestByRole(string role)
    {
        // ...
    }

    // 深拷贝消息，避免外部引用直接污染内部状态；
    // 当前会同时拷贝 Content 与 ThinkingContent
    private static RuntimeMessage CloneMessage(RuntimeMessage message)
    {
        // ...
    }
}
```

## `Providers/Interfaces/IChatProvider.cs`

```csharp
public interface IChatProvider
{
    // 非流式返回完整 assistant message
    // 当前返回结构可承载正文与 thinking
    Task<RuntimeMessage> CompleteAsync(
        IReadOnlyList<RuntimeMessage> messages,
        CancellationToken cancellationToken = default);

    // 流式返回统一分片模型
    // 当前分片结构可承载正文增量与 thinking 增量
    IAsyncEnumerable<StreamingChatChunk> StreamAsync(
        IReadOnlyList<RuntimeMessage> messages,
        CancellationToken cancellationToken = default);
}
```

## `Providers/ChatProviderFactory.cs`

```csharp
public static class ChatProviderFactory
{
    // provider 工厂入口：
    // 1. 读取 options.Vendor
    // 2. 返回 IChatProvider 实例
    // 3. ark -> ArkChatProvider
    // 4. ollama -> OllamaChatProvider
    public static IChatProvider Create(HttpClient httpClient, ChatProviderOptions options)
    {
        // ...
    }
}
```

## `Providers/Models/Ark/ArkChatProvider.cs`

```csharp
internal sealed class ArkChatProvider : IChatProvider
{
    // 共享 HttpClient
    private readonly HttpClient _httpClient;

    // provider 配置
    private readonly ChatProviderOptions _options;

    // 注入 HttpClient 与 options
    public ArkChatProvider(HttpClient httpClient, ChatProviderOptions options)
    {
        // ...
    }

    // 非流式请求：
    // 1. BuildRequestBody(stream:false)
    // 2. CreateHttpRequest
    // 3. SendAsync
    // 4. ParseAssistantMessage(content + reasoning_content)
    public async Task<RuntimeMessage> CompleteAsync(
        IReadOnlyList<RuntimeMessage> messages,
        CancellationToken cancellationToken = default)
    {
        // ...
    }

    // 流式请求：
    // 1. BuildRequestBody(stream:true)
    // 2. CreateHttpRequest
    // 3. SendAsync(ResponseHeadersRead)
    // 4. 读取 ARK SSE 流
    // 5. 解析 delta.content 与 delta.reasoning_content
    public async IAsyncEnumerable<StreamingChatChunk> StreamAsync(
        IReadOnlyList<RuntimeMessage> messages,
        [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        // ...
    }

    // 组装完整 HttpRequestMessage
    private HttpRequestMessage CreateHttpRequest(Dictionary<string, object?> body)
    {
        // ...
    }

    // 构造 ARK body JSON
    // 当前会把 assistant 历史中的 ThinkingContent 映射为 reasoning_content
    private Dictionary<string, object?> BuildRequestBody(
        IReadOnlyList<RuntimeMessage> messages,
        bool stream)
    {
        // ...
    }

    // 内部消息 -> 厂商 messages[]
    private static List<Dictionary<string, object?>> MapMessages(IReadOnlyList<RuntimeMessage> messages)
    {
        // ...
    }

    // 解析 ARK 非流式响应
    private static RuntimeMessage ParseAssistantMessage(JsonElement root)
    {
        // ...
    }

    // 读取 ARK SSE 流
    private static async IAsyncEnumerable<StreamingChatChunk> ReadStreamAsync(
        StreamReader reader,
        [EnumeratorCancellation] CancellationToken cancellationToken)
    {
        // ...
    }
}
```

## `Providers/Models/Ollama/OllamaChatProvider.cs`

```csharp
internal sealed class OllamaChatProvider : IChatProvider
{
    // 共享 HttpClient
    private readonly HttpClient _httpClient;

    // provider 配置
    private readonly ChatProviderOptions _options;

    // 注入 HttpClient 与 options
    public OllamaChatProvider(HttpClient httpClient, ChatProviderOptions options)
    {
        // ...
    }

    // 非流式请求：
    // 1. BuildRequestBody(stream:false)
    // 2. CreateHttpRequest
    // 3. SendAsync
    // 4. ParseAssistantMessage(content + thinking)
    public async Task<RuntimeMessage> CompleteAsync(
        IReadOnlyList<RuntimeMessage> messages,
        CancellationToken cancellationToken = default)
    {
        // ...
    }

    // 流式请求：
    // 1. BuildRequestBody(stream:true)
    // 2. CreateHttpRequest
    // 3. SendAsync(ResponseHeadersRead)
    // 4. 读取 Ollama NDJSON 流
    // 5. 解析 message.content 与 message.thinking
    public async IAsyncEnumerable<StreamingChatChunk> StreamAsync(
        IReadOnlyList<RuntimeMessage> messages,
        [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        // ...
    }

    // 组装完整 HttpRequestMessage
    private HttpRequestMessage CreateHttpRequest(Dictionary<string, object?> body)
    {
        // ...
    }

    // 构造 Ollama body JSON
    // 当前会把 assistant 历史中的 ThinkingContent 映射为 thinking
    private Dictionary<string, object?> BuildRequestBody(
        IReadOnlyList<RuntimeMessage> messages,
        bool stream)
    {
        // ...
    }

    // 内部消息 -> 厂商 messages[]
    private static List<Dictionary<string, object?>> MapMessages(IReadOnlyList<RuntimeMessage> messages)
    {
        // ...
    }

    // 解析 Ollama 非流式响应
    private static RuntimeMessage ParseAssistantMessage(JsonElement root)
    {
        // ...
    }

    // 读取 Ollama NDJSON 流
    private static async IAsyncEnumerable<StreamingChatChunk> ReadStreamAsync(
        StreamReader reader,
        [EnumeratorCancellation] CancellationToken cancellationToken)
    {
        // ...
    }
}
```
