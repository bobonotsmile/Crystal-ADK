# crystal-adk v0.1.0 函数职责稿

## `Session/AgentSession.cs`

```csharp
public sealed class AgentSession
{
    // 绑定运行编排器；Session 不直接发 HTTP
    private readonly AgentHost _host;

    // 保存当前会话消息历史
    private List<RuntimeMessage> _messages = new();

    // 注入 Host，创建会话对象
    internal AgentSession(AgentHost host)
    {
        _host = host;
    }

    // 对外暴露当前消息历史的只读视图
    public IReadOnlyList<RuntimeMessage> Messages => _messages;

    // 非流式入口：
    // 1. 追加 user
    // 2. 调 Host.RunAsync
    // 3. 用 result.Messages 覆盖本地历史
    public async Task<AgentRunResult> RunAsync(
        string userInput,
        CancellationToken cancellationToken = default)
    {
        // ...
    }

    // 文本流入口：
    // 1. 追加 user
    // 2. 调 Host.RunStreamingAsync
    // 3. 将 text_delta 事件桥接为 TextChunk
    // 4. 流结束后更新本地历史
    public async IAsyncEnumerable<TextChunk> StreamTextAsync(
        string userInput,
        [System.Runtime.CompilerServices.EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        // ...
    }

    // 运行事件流入口：
    // 1. 追加 user
    // 2. 调 Host.RunAsync(onEvent)
    // 3. 转发 AgentEvent
    // 4. 流结束后更新本地历史
    public async IAsyncEnumerable<AgentEvent> StreamEventsAsync(
        string userInput,
        [System.Runtime.CompilerServices.EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        // ...
    }

    // 清空会话消息历史
    public void Reset()
    {
        // ...
    }
}
```

## `Core/AgentHost.cs`

```csharp
public sealed class AgentHost
{
    // 工具注册中心与执行入口
    private readonly ToolHub _toolHub;

    // 底层模型 provider 抽象
    private readonly IChatProvider _chatProvider;

    // Host 默认配置：system prompt / max rounds
    private readonly AgentHostOptions _options;

    // 简化构造；自动 new ToolHub
    public AgentHost(IChatProvider chatProvider, AgentHostOptions? options = null)
    {
        // ...
    }

    // 完整构造；允许外部注入 ToolHub
    public AgentHost(ToolHub toolHub, IChatProvider chatProvider, AgentHostOptions? options = null)
    {
        // ...
    }

    // 创建会话对象
    public AgentSession CreateSession()
    {
        // ...
    }

    // 非流式运行：
    // 1. 校验 request
    // 2. BuildInitialMessages
    // 3. 获取工具描述
    // 4. 调 provider.CreateCompletionAsync
    // 5. assistant 入历史
    // 6. 若有 tool_calls 则执行 tool loop
    // 7. 返回最终 AgentRunResult
    public async Task<AgentRunResult> RunAsync(
        AgentRunRequest request,
        Func<AgentEvent, CancellationToken, Task>? onEvent = null,
        CancellationToken cancellationToken = default)
    {
        // ...
    }

    // 流式运行：
    // 1. 校验 request
    // 2. BuildInitialMessages
    // 3. 调 provider.StreamCompletionAsync
    // 4. 聚合 TextDelta / tool call 快照
    // 5. 流结束时组装 assistant
    // 6. 若有 tool_calls 则继续 tool loop
    internal async Task<AgentRunResult> RunStreamingAsync(
        AgentRunRequest request,
        Func<AgentEvent, CancellationToken, Task>? onEvent = null,
        CancellationToken cancellationToken = default)
    {
        // ...
    }

    // 运行前消息预处理：
    // 1. 深拷贝输入消息
    // 2. 如需则插入默认 system
    private List<RuntimeMessage> BuildInitialMessages(IReadOnlyList<RuntimeMessage> incomingMessages)
    {
        // ...
    }

    // 深拷贝单条 RuntimeMessage
    private static RuntimeMessage CloneMessage(RuntimeMessage message)
    {
        // ...
    }

    // 统一事件发射
    private static async Task EmitStreamEventAsync(
        Func<AgentEvent, CancellationToken, Task>? onEvent,
        AgentEvent evt,
        CancellationToken cancellationToken)
    {
        // ...
    }

    // 工具结果序列化为 role=tool 的文本内容
    private static string SerializeToolContent(object? value)
    {
        // ...
    }

    // 构建工具调用签名，做重复调用检测
    private static string BuildToolCallSignature(string toolName, IReadOnlyDictionary<string, object?> arguments)
    {
        // ...
    }

    // 对参数对象做稳定归一化
    private static object? NormalizeForSignature(object? value)
    {
        // ...
    }
}
```

## `Core/ToolHub.cs`

```csharp
public interface IAgentTool
{
    // 对外暴露工具描述
    AgentToolDescriptor Descriptor { get; }

    // 执行工具
    Task<object?> InvokeAsync(Dictionary<string, object?> arguments, CancellationToken cancellationToken);
}

public sealed class ToolHub
{
    // 工具注册表，按名称索引
    private readonly Dictionary<string, IAgentTool> _tools = new(StringComparer.OrdinalIgnoreCase);

    // 注册工具；校验空值、空名称、重名
    public void Register(IAgentTool tool)
    {
        // ...
    }

    // 导出全部工具描述
    public IReadOnlyList<AgentToolDescriptor> GetDescriptors()
    {
        // ...
    }

    // 通过工具名执行工具
    public async Task<object?> InvokeAsync(string name, Dictionary<string, object?> arguments, CancellationToken cancellationToken)
    {
        // ...
    }
}
```

## `Providers/ChatProvider.cs`

```csharp
public static class ChatProvider
{
    // 工厂入口：
    // 1. 读取 Vendor
    // 2. 转换成 vendor options
    // 3. 返回具体 provider
    public static IChatProvider Create(HttpClient httpClient, ChatProviderSettings settings)
    {
        // ...
    }

    // 统一 settings -> ARK 专用 options
    private static ArkChatProviderOptions ToArkOptions(ChatProviderSettings settings)
    {
        // ...
    }

    // 统一 settings -> Ollama 专用 options
    private static OllamaChatProviderOptions ToOllamaOptions(ChatProviderSettings settings)
    {
        // ...
    }
}
```

## `Providers/ProviderJson.cs`

```csharp
internal static class ProviderJson
{
    // 将 arguments 文本解析为 Dictionary<string, object?>
    public static Dictionary<string, object?> ParseArguments(string text)
    {
        // ...
    }

    // 将 JsonElement.Object 转为字典
    public static Dictionary<string, object?> ConvertObject(JsonElement element)
    {
        // ...
    }

    // 将 JsonElement 递归转换为 CLR 值
    public static object? ConvertValue(JsonElement element)
    {
        // ...
    }
}
```

## `Providers/Ark/ArkChatProvider.cs`

```csharp
public sealed class ArkChatProvider : IChatProvider
{
    // 共享 HttpClient
    private readonly HttpClient _httpClient;

    // ARK 专用配置
    private readonly ArkChatProviderOptions _options;

    // 注入 HttpClient 与 options
    public ArkChatProvider(HttpClient httpClient, ArkChatProviderOptions options)
    {
        // ...
    }

    // ARK 非流式：
    // 1. BuildRequestBody(stream:false)
    // 2. CreateRequest
    // 3. SendAsync
    // 4. ParseCompletionMessage
    public async Task<RuntimeMessage> CreateCompletionAsync(
        IReadOnlyList<RuntimeMessage> messages,
        IReadOnlyList<AgentToolDescriptor> tools,
        CancellationToken cancellationToken)
    {
        // ...
    }

    // ARK 流式：
    // 1. BuildRequestBody(stream:true)
    // 2. CreateRequest
    // 3. 读取 SSE
    // 4. 转为 StreamingChatChunk
    public async IAsyncEnumerable<StreamingChatChunk> StreamCompletionAsync(
        IReadOnlyList<RuntimeMessage> messages,
        IReadOnlyList<AgentToolDescriptor> tools,
        [EnumeratorCancellation] CancellationToken cancellationToken)
    {
        // ...
    }

    // 创建 HTTP 请求对象
    private HttpRequestMessage CreateRequest(Dictionary<string, object?> body)
    {
        // ...
    }

    // 组装 ARK body JSON
    private Dictionary<string, object?> BuildRequestBody(
        IReadOnlyList<RuntimeMessage> messages,
        IReadOnlyList<AgentToolDescriptor> tools,
        bool stream)
    {
        // ...
    }

    // 内部消息 -> ARK messages[]
    private static List<Dictionary<string, object?>> MapMessages(IReadOnlyList<RuntimeMessage> messages)
    {
        // ...
    }

    // 工具描述 -> ARK tools[]
    private static List<Dictionary<string, object?>> MapTools(IReadOnlyList<AgentToolDescriptor> tools)
    {
        // ...
    }

    // 完整响应 -> RuntimeMessage
    private static RuntimeMessage ParseCompletionMessage(JsonElement root)
    {
        // ...
    }

    // 完整响应中的 tool_calls -> RuntimeToolCall[]
    private static List<RuntimeToolCall> ParseCompletionToolCalls(JsonElement toolCalls)
    {
        // ...
    }

    // 流式 delta 中的 tool_calls -> RuntimeToolCall[]
    private static List<RuntimeToolCall> ParseDeltaToolCalls(JsonElement toolCalls)
    {
        // ...
    }
}
```

## `Providers/Ollama/OllamaChatProvider.cs`

```csharp
public sealed class OllamaChatProvider : IChatProvider
{
    // 共享 HttpClient
    private readonly HttpClient _httpClient;

    // Ollama 专用配置
    private readonly OllamaChatProviderOptions _options;

    // 注入 HttpClient 与 options
    public OllamaChatProvider(HttpClient httpClient, OllamaChatProviderOptions options)
    {
        // ...
    }

    // Ollama 非流式：
    // 1. BuildRequestBody(stream:false)
    // 2. CreateRequest
    // 3. SendAsync
    // 4. ParseCompletionMessage
    public async Task<RuntimeMessage> CreateCompletionAsync(
        IReadOnlyList<RuntimeMessage> messages,
        IReadOnlyList<AgentToolDescriptor> tools,
        CancellationToken cancellationToken)
    {
        // ...
    }

    // Ollama 流式：
    // 1. BuildRequestBody(stream:true)
    // 2. CreateRequest
    // 3. 读取 NDJSON
    // 4. 转为 StreamingChatChunk
    public async IAsyncEnumerable<StreamingChatChunk> StreamCompletionAsync(
        IReadOnlyList<RuntimeMessage> messages,
        IReadOnlyList<AgentToolDescriptor> tools,
        [EnumeratorCancellation] CancellationToken cancellationToken)
    {
        // ...
    }

    // 创建 HTTP 请求对象
    private HttpRequestMessage CreateRequest(Dictionary<string, object?> body)
    {
        // ...
    }

    // 组装 Ollama body JSON
    private Dictionary<string, object?> BuildRequestBody(
        IReadOnlyList<RuntimeMessage> messages,
        IReadOnlyList<AgentToolDescriptor> tools,
        bool stream)
    {
        // ...
    }

    // 内部消息 -> Ollama messages[]
    private static List<Dictionary<string, object?>> MapMessages(IReadOnlyList<RuntimeMessage> messages)
    {
        // ...
    }

    // 工具描述 -> Ollama tools[]
    private static List<Dictionary<string, object?>> MapTools(IReadOnlyList<AgentToolDescriptor> tools)
    {
        // ...
    }

    // 完整响应 -> RuntimeMessage
    private static RuntimeMessage ParseCompletionMessage(JsonElement root)
    {
        // ...
    }

    // tool_calls -> RuntimeToolCall[]
    private static List<RuntimeToolCall> ParseToolCalls(JsonElement toolCalls)
    {
        // ...
    }

    // arguments 归一化为 Dictionary<string, object?>
    private static Dictionary<string, object?> NormalizeArguments(JsonElement node)
    {
        // ...
    }
}
```

